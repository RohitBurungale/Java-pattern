How the pattern works (very simple)

The program uses two loops:

1.One loop for rows

2.One loop for columns

In each position, it checks:

If it's the first row, last row, first column, or last column → print *

Otherwise → print space

This creates a hollow rectangle.
